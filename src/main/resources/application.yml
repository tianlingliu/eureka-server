server:
  port: 80

spring:
  application:
    name: eureka-server
  boot:
    admin:
      client:
        url: http://localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: "*"